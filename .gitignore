# Node dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files & logs
*.log
*.tmp

# SQLite journal & lock files
*.db-journal
*.db-shm
*.db-wal

# Automatic local database backups (rotated copies next to the live .db —
# not meant to live in git; download a real backup via the app's "Unduh
# Cadangan" button or copy data/stock_history.db somewhere safe instead).
# Cimahi runs as a separate deployment (see docker-compose.yml) with its own
# data-cimahi/ directory, so its backups need the same rule.
data/backups/
data-cimahi/backups/

# Operating System files
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Daily uploaded reports data
data harian/
1308 vcr.csv
14-09-2026.xls

